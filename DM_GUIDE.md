# DM 操作手册 — 繁星 D&D 小助手

> 面向 DM 的实操指南：我想做 X，应该怎么做？
> 标注了 ✅ 完全可用 / ⚠️ 部分可用 / ❌ 暂不支持

---

## 一、内容创建

### 1. 我想新增一个种族

**状态：✅ 完全可用**

1. 打开网站 → 底部导航 → 更多 → 种族
2. 点"新建种族"
3. 填写：名称、属性加值、特性列表
4. 每个特性可以配 BUFF 效果（走效果编辑器）
5. 保存

**注意：** 种族特性效果会作为虚拟 BUFF 自动注入计算管线。

---

### 2. 我想新增一个职业

**状态：⚠️ 部分可用**

1. 打开网站 → 底部导航 → 更多 → 数据维护
2. 选"职业资料库" → 粘贴 Markdown 文本
3. 格式要求：
   - 第一行 `# 职业名`
   - 概览表 `| 项 | 值 |`（生命骰、豁免熟练、护甲熟练、武器熟练、技能选项）
   - 等级表 `| 等级 | 特性 |`
   - 特性详情 `### 特性名（N 级）`
4. 点"预览"检查解析结果 → "确认保存"

**当前缺口：**
- ❌ 保存后角色创建下拉菜单**选不到**这个职业（下拉只读内置列表）
-  粘贴解析器**不支持** spellcasting（施法）、subclasses（子职业）、resource rules（资源规则）
- ❌ 自定义职业**没有自动资源**（如怒气、气点等）

** workaround：** 如果急需使用，可以让玩家手动在角色卡上填写职业名，系统会尝试解析特性。

---

### 3. 我想新增一个专长

**状态：⚠️ 部分可用**

1. 数据维护 → 专长资料库 → 粘贴 Markdown
2. 格式：`# 专长名` + 分类/先决条件/描述
3. 预览 → 确认保存

**当前缺口：**
- ❌ 保存后专长选择弹窗**看不到**这个专长（弹窗只读内置列表）
- ❌ 专长本身**没有机械效果**（只是文字描述）

**配效果的方法（需管理员）：**
1. 在角色卡上打开专长选择弹窗
2. 找到目标专长 → 点"默认 BUFF"
3. 用效果编辑器配置效果
4. 保存后，所有玩家选该专长时自动获得此 BUFF

---

### 4. 我想新增一个物品/武器

**状态：✅ 完全可用**

1. 数据维护 → 物品资料库 → "添加物品"
2. 选物品类型（武器/护甲/道具等）
3. 填写：名称、稀有度、是否需要同调、类型专属字段（武器伤害/AC等）
4. 保存

**给物品加附魔效果：**
1. 编辑物品 → "附魔效果"区域
2. 点"添加效果" → 打开效果编辑器
3. 选分类 → 选 effectType → 填 scope → 填 value
4. 保存

---

### 5. 我想给武器加一个主动技能

**状态：✅ 完全可用**

1. 编辑物品 → "附魔效果" → 添加效果
2. 分类选 **"主动释放"** → effectType 选 **"释放效果（charge_item）"**
3. 配置：
   - 资源类型：选消耗什么（充能/法术位/职业资源/无）
   - 消耗模式：固定值 或 自由（×环位）
   - 充能数 / 恢复方式（短休/长休/黎明）
   - 动作类型：动作/附赠动作/反应
   - 子效果：添加伤害骰/治疗/变形/召唤/BUFF 等
4. 保存

**释放流程：** 物品装备后 → 战斗手段卡片区出现"释放"按钮 → 点击 → 确认消耗 → 执行效果

---

## 二、效果配置

### 6. 我想创建一个临时 BUFF（增益/减益）

**状态：✅ 完全可用**

1. 角色主页 → BUFF 状态面板 → "新建 BUFF"
2. 填写：名称、持续时间
3. 添加效果模块（效果编辑器）：
   - 选分类（属性/攻击/防御/施法/主动释放/熟练/自定义）
   - 选 effectType（如 ac_bonus、damage_bonus、skill_bonus 等）
   - 选 scope（全局/近战攻击/远程攻击/特定武器等）
   - 填 value（数值 或 公式如 `{ref: 'abilityModifier', ability: 'str'}`）
4. 保存 → 立即参与计算

**常用效果速查：**
| 想要什么 | effectType | scope 示例 |
|---------|-----------|-----------|
| AC +2 | ac_bonus | global |
| 攻击 +3 | attack_bonus | global 或 melee_attack |
| 伤害 +5 | damage_bonus | global 或 melee_weapon |
| 力量 +4 | ability_score | global, scopeDetail: ['str'] |
| 技能 +2 | skill_bonus | global, scopeDetail: ['athletics'] |
| 额外骰子伤害 | extra_damage_dice | melee_attack |
| 临时HP | temp_hp | global |
| 速度 +10尺 | base_speed_increment | global |
| 抗性（某伤害类型） | damage_type_relation | global |

---

### 7. 我想修改一个专长的效果

**状态：✅ 可用（需管理员）**

1. 角色主页 → 专长区 → 打开专长选择弹窗
2. 找到目标专长 → 点"默认 BUFF"按钮
3. 用效果编辑器配置/修改效果
4. 保存 → 该专长对所有玩家生效

**注意：** 这是模块级配置，影响所有使用此专长的角色。

---

### 8. 我想修改一个职业特性的效果

**状态：✅ 可用（需管理员）**

1. 角色主页 → 职业特性区
2. 找到目标特性 → 点编辑效果
3. 用效果编辑器配置
4. 保存

---

## 三、法术相关

### 9. 我想修改一个法术的伤害

**状态：❌ 暂不支持**

法术目前是**只读数据库记录**，没有编辑 UI。

**当前 workaround：**
- 通过效果编辑器创建一个同名 BUFF，手动配置伤害效果
- 玩家在战斗手段区手动释放这个 BUFF

**未来方向：** 让法术条目拥有 effects[] 字段，或通过 charge_item 桥接。

---

### 10. 我想让法术消耗法术位

**状态：✅ 已支持**

法术释放时自动消耗对应环位的法术位。DM 可以在角色卡上手动调整法术位上限和当前值。

---

## 四、资源管理

### 11. 我想查看/调整角色的资源

**状态：✅ 可用**

角色主页 → 资源区显示所有职业资源（怒气、气点、法术位等）。
- 点击 +/- 调整当前值
- 长休/短休按钮自动恢复

**注意：** 资源类型和恢复规则是硬编码的，DM 不能新增资源类型。

---

### 12. 我想给自定义职业配资源规则

**状态：❌ 暂不支持**

资源规则完全硬编码在 `classResourceRules.js`，约 30 条规则对应内置职业。自定义职业无法自动获得资源。

**workaround：** 玩家手动在资源区添加自定义资源条目（如果 UI 支持），或 DM 通过效果编辑器模拟资源效果。

---

## 五、高级配置

### 13. 我想创建一个选择型特性（多选一）

**状态：⚠️ 部分可用**

效果编辑器支持 `choice` 类型效果：
1. 添加效果 → 分类选"选项效果" → effectType 选"choice"
2. 配置多个选项，每个选项有自己的效果列表
3. 玩家在角色卡上选择其中一个选项

**注意：** 选择后效果立即注入计算管线。

---

### 14. 我想配置护盾池

**状态：✅ 可用**

1. 编辑物品 → 附魔效果 → 添加效果
2. 分类选"防御/生存" → effectType 选"shield_pool"
3. 配置：当前层数、最大层数、阈值以上附加效果
4. 保存

**机制：** 护盾池层数直接参与 AC 计算（作为 ac_bonus 注入），耗尽时 AC 自动降低。

---

### 15. 我想配置法术位消耗模式

**状态：✅ 可用**

在 charge_item 效果中：
- **固定消耗：** 每次释放消耗固定环位（如"消耗一个三环法术位"）
- **自由消耗：** 基础值 × 消耗环位（如"每环位 1d6 伤害"）

配置路径：效果编辑器 → charge_item → 消耗模式选择

---

## 六、权限说明

| 操作 | 普通玩家 | 管理员（DM） |
|------|---------|------------|
| 创建临时 BUFF | ✅ | ✅ |
| 配置专长默认 BUFF | ❌ | ✅ |
| 配置职业特性 BUFF | ❌ | ✅ |
| 创建自定义种族 | ✅ | ✅ |
| 创建自定义物品 | ✅ | ✅ |
| 数据维护（批量导入） | ❌ | ✅ |

---

## 七、常见问题

### Q: 我创建的职业/专长为什么选不到？
A: 当前角色创建下拉菜单只读内置列表。这是已知缺口，优先级高，后续会修复。

### Q: 法术能不能像 BUFF 一样配置效果？
A: 目前不能。法术是纯文本记录。未来会让法术接入效果编辑器。

### Q: 效果编辑器里的"公式"怎么用？
A: value 字段可以填：
- 纯数字：如 `2`、`-1`
- 公式对象：如 `{ref: 'abilityModifier', ability: 'str'}`（力量调整值）
- 公式对象：如 `{ref: 'classLevel'}`（职业等级）
- 公式对象：如 `{ref: 'proficiencyBonus'}`（熟练加值）

### Q: 我配的效果没有生效？
A: 检查：
1. 效果是否已保存（点"保存"而非"取消"）
2. scope 是否匹配（global 全局生效，melee_attack 只对近战攻击生效）
3. 是否有冲突效果（高优先级覆盖低优先级）
4. 刷新页面重试

---

*最后更新：2026-09-01*
*本文档基于当前代码能力编写，缺口部分标注了 workaround 和未来方向*
