# D&D Team App — 终极功能规划

> 最后更新：2026-08-26（全面重写版）
>
> 本文档是网站的最终功能蓝图，包含：现状盘点、UI/UX 审查、七大支柱详述、DM 全编辑体系、分步实施计划。

---

## 一、项目现状总览

### 1.1 代码规模

| 层级 | 文件数 | 总行数 | 说明 |
|------|--------|--------|------|
| 页面 (pages/) | 14 | ~15,300 | 含 6 个备份文件 |
| 组件 (components/) | 44 | ~27,300 | 最大：CombatStatus 5055 / BuffForm 3906 / Warehouse 3065 |
| 数据 (data/) | 19 | ~9,300 | 法术/职业/专长/武器/生物库 |
| 工具库 (lib/) | 73 | ~11,400 | 存储/公式/效果管线/同步 |
| 钩子 (hooks/) | 6 | ~1,300 | BUFF 计算引擎 963 行 |
| **合计** | **156** | **~64,600** | |

### 1.2 页面清单

| 路由 | 页面 | 行数 | 功能 |
|------|------|------|------|
| `/` | Dashboard | 549 | 模组管理 + 角色列表 + 快照备份 |
| `/characters/:id` | CharacterSheet | 2887 | 角色主卡（属性/BUFF/战斗/装备/特性/专长） |
| `/character-spells` | CharacterSpellsPage | 56 | 角色法术管理 |
| `/warehouse` | Warehouse | 3065 | 团队仓库（公共次元袋 + 秘法箱 + 货币 + 制作） |
| `/spells` | Spells | 849 | 法术大全 |
| `/more` | More | 64 | 导航枢纽（法术/房规/模板库/生物库） |
| `/more/house-rules` | HouseRules | 800 | DM 规则文案编辑 |
| `/more/data` | DataMaintain | 834 | 物品/专长/职业数据维护 |
| `/more/module-library` | ModuleLibrary | 579 | 模组级 BUFF/物品模板 |
| `/more/creature-library` | CreatureLibraryManager | 268 | 生物库管理 |

### 1.3 底部导航（5 个 Tab）

首页 → 我的角色 → 角色法术 → 团队仓库 → 更多

底栏同时集成完整骰子计算器（数字键 + 骰子键 + 运算符 + 优势/劣势 + 3D 动画）。

### 1.4 各系统完成度

| 系统 | 完成度 | 核心状态 |
|------|--------|---------|
| BUFF 系统 | ~70% | 编辑器 + 计算引擎 + 选择型 + 变身 + 效果管线完整；仅德鲁伊有职业特性 BUFF，其余 11+ 职业为空 |
| 主动技能 | ~15% | 数据模型已设计，无实际注册表和 UI |
| 战斗系统 | ~40% | 武器攻击卡片 + 法术管理 + 3D 骰子已有；流程不完整 |
| 养成系统 | ~50% | 经验/等级/装备/货币/制作/短长休已有；缺升级引导/商店 |
| 日志系统 | ~5% | 无任何实现 |
| 模组与房规 | ~35% | 模组 CRUD + 快照 + BUFF 模板 + 规则覆盖已有；缺可视化开关/数据隔离 |
| 数据持久化 | ~60% | Supabase + localStorage + IndexedDB + GitHub 备份；缺自动备份/版本历史/离线 |

---

## 二、UI/UX 全面审查

### 2.1 CharacterSheet（角色主卡）

**当前结构**：单列垂直堆叠 8 个区块。

| 区块 | 问题 | 严重度 |
|------|------|--------|
| 个人信息 | 5 列外观网格在小屏挤压严重，输入框过窄 | 中 |
| 经验与等级 | 等级在 3+ 处重复显示（经验等级、职业等级步进器、顶部摘要） | 低 |
| BUFF 状态 | BUFF 效果分散在 3 处配置：手动 BUFF 在 BuffManager、职业特性 BUFF 在特性齿轮弹窗、专长 BUFF 在专长选择弹窗 | 高 |
| 战斗状态 | CombatStatus 5055 行，战斗手段区占 540 行 JSX，每张卡片内联计算 80 行派生数据 | 高 |
| 装备与背包 | 钱包嵌在背包最底部，长列表时被推到视野外 | 中 |
| 职业特性/专长 | 两列布局合理，但魔能祈唤/战斗风格选择器嵌在特性卡片内部展开 | 低 |

**改进方向**：
- 战斗手段卡片拆分为独立子组件（WeaponCard / SpellCard / ItemCard）
- 派生数据计算集中到 useMemo，不再每卡片重复
- 外观网格改为自适应列（小屏 3 列、中屏 5 列）
- BUFF 来源统一入口（见 Phase 3 DM 编辑模式）

### 2.2 CombatStatus（战斗状态面板）

| 区块 | 问题 | 严重度 |
|------|------|--------|
| 添加战斗手段弹窗 | 5 步子向导挤在一个 550 行弹窗里，武器表单 210 行 | 高 |
| 武技设置弹窗 | 270 行，站姿/打击行重复渲染 | 中 |
| 状态效果 | 力竭下拉 + 条件标签 + 描述行，信息密度合理 | — |
| 职业资源 | 5 列网格紧凑合理，恢复标记清晰 | — |
| 法术位 | 点阵指示器直观好用 | — |

**改进方向**：
- 添加战斗手段弹窗拆分为分步组件（TypeStep / WeaponStep / SpellStep / ItemStep）
- 武器 extra dice 编辑器独立为小组件
- 武技站姿/打击行合并为统一渲染函数

### 2.3 BuffForm（BUFF 编辑器）

| 区块 | 问题 | 严重度 |
|------|------|--------|
| 主表单 | 仅 3-4 字段（来源/时间/归类/效果列表），紧凑合理 | — |
| 效果弹窗 (EffectModuleModal) | 大类选择 15-20 项需滚动；范围 + 值编辑器根据类型完全不同 | 中 |
| 多层弹窗 | 主表单 → 效果弹窗 → 选择型子效果弹窗，最深 3 层 | 高 |
| 熟练检查表 | 工具有 27 个 checkbox 全部平铺，占大量垂直空间 | 中 |
| 变身编辑器 | 单弹窗内字段极多（模式/子职/生物/能力/AC/HP/资源），无分步或折叠 | 高 |

**改进方向**：
- 大类选择改为分组网格（2 列按钮组）替代长下拉
- 熟练检查表改为搜索多选（带已选计数）
- 变身编辑器拆分为 2 步（基础设置 → 高级覆盖）
- 3 层弹窗问题：选择型子效果改为内嵌编辑区而非再开弹窗

### 2.4 Warehouse（团队仓库）

| 区块 | 问题 | 严重度 |
|------|------|--------|
| 公共次元袋 | 按角色按模块分袋，结构清晰 | — |
| 秘法箱 | 体积容量追踪 + 超容警告，实用 | — |
| 拖拽交互 | 无处不在（袋间/箱间/角色间），学习成本高 | 中 |
| 嵌套袋中袋 | 递归渲染，层级可能很深 | 低 |

**改进方向**：
- 增加拖拽目标高亮提示
- 嵌套层级限制或增加面包屑导航

### 2.5 全局 UI 原则（后续开发遵循）

1. **信息紧凑**：同类信息一行多列，减少纵向空白
2. **模块化区隔**：大模块用卡片壳（渐变背景 + rounded-xl），小模块用分隔线
3. **弹窗不超 3 层**：如需更深，改为页内编辑或分步向导
4. **下拉不超 10 项**：超过则改为搜索选择器或分组按钮
5. **输入框宽度合理**：数字输入 w-20~w-28，文本输入不超 w-48，不撑满全宽
6. **步进器紧凑**：w-[6.25rem] 容器，± 按钮紧贴数值
7. **同一信息不分割**：HP 就在 HP 区，不要拆到两个面板

---

## 三、七大支柱详述

### 支柱 1：BUFF 系统（被动增益层）

#### 核心理念

BUFF 是网站的**数据地基**。不是所有职业特性都需要做成 BUFF——只有满足以下条件之一的才进入 BUFF 编辑器：

1. **需要用户做选择**：互斥特性（如原初职能：术师/卫士）
2. **需要修改用户数据**：改变属性值/AC/速度/熟练项等数值
3. **需要主动动作应对**：动作/附赠动作/反应产生即时效果
4. **获得额外法术**：特性带来的额外法术或戏法

纯描述性特性只在职业特性栏展示，不进入 BUFF。

#### 设计铁律

- BUFF 编辑器 → BUFF 状态栏 → 数值计算，唯一数据通路
- 来源标签显示特性名（如"荒野变形""结社形态"而非"德鲁伊"）
- 描述只写核心功能，细则看职业特性面板
- 有真实 BUFF 效果时不重复添加 custom_condition 描述同一数值效果
- DM 可通过页面直接修改任何 BUFF 效果

#### 已完成

- BUFF 编辑器：10 大类 50+ 种效果类型
- 计算引擎：963 行，覆盖 AC/攻击/豁免/速度/抗性/优势/变身
- 选择型 BUFF：互斥选择完整数据流
- 变身系统：荒野变形模式含属性覆盖/临时HP/天生武器
- 效果管线：统一提取 + 合并，DM 补丁优先级最高
- 专长默认 BUFF：~90 个专长全覆盖
- 德鲁伊职业特性 BUFF：14 个条目精简为 2 个（仅保留真实数值效果）✅
- BUFF 状态栏精简：来源标签显示特性名，描述一行摘要 ✅

#### 待完成

- 补全 11+ 职业的数值型 BUFF
- 种族/血统 BUFF、背景 BUFF、武器精通 BUFF
- 魔法物品被动效果自动进入 BUFF 管线
- 野兽/生物库用户录入（Step 36）
- 召唤系统（Step 37）

---

### 支柱 2：主动技能系统（即时操作层）

#### 核心理念

被动增益之外的**主动使用能力**。点击按钮 → 检查条件 → 扣除资源 → 执行效果 → 记录状态。

#### 动作类型

| 类型 | 示例 | 消耗 |
|------|------|------|
| 附赠动作 | 荒野变形、回气、药水 | 附赠动作 + 资源 |
| 主动动作 | 援助术、圣疗 | 主动作 + 资源 |
| 反应 | 巫师杀手 | 反应 + 资源 |
| 特殊触发 | 灵感、幸运 | 特殊资源 |
| 充能物品 | 魔杖/卷轴 | 充能次数 |

#### 数据模型

```
ActiveAbility = {
  id, name, source, sourceKey,
  actionType,        // action / bonus_action / reaction / special
  costType,          // class_resource / charge / none
  costKey, costAmount,
  cooldown,          // short_rest / long_rest / dawn / none
  effects[],         // apply_buff / transform / heal / reroll / ...
  conditions[],      // 使用前置条件
  icon, description,
  needsInteraction,  // none / select_creature / select_target / confirm
}
```

#### 当前状态

数据模型已设计，注册表 (ACTIVE_ABILITY_REGISTRY) 和 UI 组件 (ActionPanel) 尚未实现。

---

### 支柱 3：战斗系统（手段施展层）

#### 核心理念

将 BUFF 增强后的手段集中施展：武器攻击、法术施展、道具使用。

#### 数据流

```
BUFF 系统（被动加成）→ 战斗手段（武器/法术/道具）→ 战斗日志（结果）→ 角色数值（HP/资源）
```

#### 已完成

- 战斗手段面板：武器/法术/爆炸物/卷轴/充能物品卡片
- 武器攻击计算：命中加值、伤害骰、属性修正
- 法术管理：已准备/已知法术列表，法术攻击/DC
- 3D 骰子动画：Three.js 物理骰子
- 快捷投骰：底栏骰子计算器

#### 待完成

- 完整攻击流程（选武器 → 投骰 → 判定 → 伤害 → 应用加成）
- 法术施展流程（选法术 → 投骰/豁免 → 消耗法术位）
- 战斗日志面板
- 状态效果规则自动应用

---

### 支柱 4：养成系统（成长循环层）

#### 已完成

经验/等级、多职业/多等级、属性编辑、装备/背包/负重、七种货币/团队金库/钱包、团队仓库/秘法箱/次元袋、魔法制作（奥法工坊）、法术管理、短休/长休。

#### 待完成

升级引导、商店系统、战利品分配、角色成长时间线。

---

### 支柱 5：日志系统（冒险记载层）

#### 设计规划

五种日志类型：NPC 记录、事件记录、地点记录、任务记录、宝藏记录。

每条记录包含：标题、内容、标签、关联角色、会话标记、DM 私密字段。

DM 视角可创建秘密条目；玩家视角只看公开内容。支持富文本编辑和搜索。

#### 当前状态

无任何实现。全新模块。

---

### 支柱 6：模组与房规系统（环境配置层）

#### 已完成

模组 CRUD + 排序、模组快照备份/恢复、IndexedDB + GitHub 存档、BUFF 模板库、规则正文覆盖 (HouseRules)、DM 默认 BUFF 补丁、自定义物品/专长/职业 (DataMaintain)、Supabase 实时同步。

#### 待完成

房规可视化开关、模组间数据完全隔离、模组迁移工具、自制内容管理、房规模板。

---

### 支柱 7：数据持久化（安全存储层）

#### 当前架构

```
Supabase（主存储）→ characters / campaign_modules / team_warehouse / team_currency / custom_library
localStorage（回退/缓存）→ starlight_characters / starlight_modules / ...
IndexedDB（大对象）→ 模组快照 / 模组存档
GitHub 备份（远程灾备）→ JSON 导出
```

#### 待完成

自动备份策略、版本历史、离线模式 (Service Worker)、数据导出/导入、冲突解决。

---

## 四、DM 零代码全编辑体系

### 4.1 核心目标

网站功能全部实现后，修改内部数据或文案只需要通过网页平台即刻完成。DM 是内容的管理者，玩家是内容的享用者。

### 4.2 实现方式

**统一编辑模式开关**：页面顶部（或 DM 面板）有一个"开启编辑"按钮，仅 DM 可见。

**编辑态 UI**：开启后，所有可编辑条目旁出现铅笔（编辑）或齿轮（配置）图标。

### 4.3 内容管理范围

| 内容类型 | 可编辑项 |
|----------|---------|
| 专长 | 名称、描述、默认 BUFF 效果、前置条件 |
| 职业特性 | 名称、等级、描述、BUFF 效果、是否选择型 |
| 武器 | 名称、伤害骰、属性、特性、精通效果 |
| 法术 | 名称、等级、学派、施法时间、距离、持续、效果描述 |
| 种族/血统 | 名称、属性加值、特性列表、BUFF 效果 |
| 背景 | 名称、技能熟练、工具熟练、语言、特性描述 |
| 魔法物品 | 名称、稀有度、同调要求、充能、BUFF 效果 |
| 怪物/NPC | 名称、AC、HP、攻击、特性 |
| 房规 | 启用/禁用规则条目、自定义规则文本 |

### 4.4 数据存储

DM 编辑内容存储在模组级别（localStorage 或 Supabase），不同模组互不污染。硬编码数据作为"出厂默认"，DM 覆盖后以 DM 数据为准（与现有 BUFF 补丁优先级一致）。

### 4.5 设计约束

- 编辑操作不删除硬编码默认数据，只做覆盖（可随时重置回默认）
- 批量操作需二次确认
- 所有编辑操作记录变更日志（可选，Phase 7 版本历史）

---

## 五、七大支柱协作关系

```
                    ┌──────────────┐
                    │  模组与房规   │ ← 定义规则环境
                    └──────┬───────┘
                           │ 约束
              ┌────────────┼────────────┐
              ▼            ▼            ▼
        ┌──────────┐ ┌──────────┐ ┌──────────┐
        │ BUFF 系统 │ │ 主动技能  │ │ 日志系统  │
        │ (被动增益) │ │ (即时操作) │ │ (冒险记载) │
        └─────┬────┘ └────┬─────┘ └──────────┘
              │           │
              │ 注入数值   │ 消耗资源/执行效果
              ▼           ▼
        ┌──────────────────────┐
        │      战斗系统         │ ← 集中施展
        │  (武器/法术/道具)     │
        └──────────┬───────────┘
                   │ 结果
                   ▼
        ┌──────────────────────┐
        │      养成系统         │ ← 成长循环
        │ (升级/金钱/物品/制作)  │
        └──────────┬───────────┘
                   │ 数据
                   ▼
        ┌──────────────────────┐
        │     数据持久化        │ ← 安全存储
        │ (Supabase/本地/备份)  │
        └──────────────────────┘
```

---

## 六、关键设计原则

1. **BUFF 是唯一的数据真相**：所有数值增益必须通过 BUFF 管线
2. **DM 零代码全权编辑**：所有游戏内容的增删改通过网页界面完成
3. **主动技能消耗真实资源**：每次使用必须扣除对应资源
4. **模组是最高级别的环境**：模组设置决定哪些规则生效
5. **数据安全高于一切**：任何操作都不应导致数据丢失
6. **渐进式复杂度**：简单的事简单做，复杂的事能做到
7. **玩家全自助体验**：玩家无需理解技术细节，点击即用

---

## 七、设计约束与教训

> 从实际开发中提炼的硬性规则，后续开发必须遵守，避免重复踩坑。

### 7.1 BUFF 来源标签规则（Step 1 教训）

**问题**：初始实现将来源标签设为职业名（`f.sourceClass`），导致同一职业的所有特性 BUFF 都显示"德鲁伊"，无法区分。

**规则**：BUFF 来源标签必须使用特性名（`f.name`），如"荒野变形""结社形态"。每个特性独立标识，玩家一眼能区分不同来源。

### 7.2 真实效果与描述文本去重规则（Step 1 教训）

**问题**：原力蛮击同时出现 custom_condition（"每回合一次额外1d8元素伤害"）和 extra_damage_dice 效果，信息重复。月辉形态同理。

**规则**：
- 当特性已有真实 BUFF 效果（参与数值计算）时，不再添加描述相同数值的 custom_condition
- custom_condition 仅用于描述无法量化的规则文本（如"可传送同伴"）
- 选择型特性的 `getEffects()` 只返回真实效果，不附带描述文本（描述由特性面板本身展示）

### 7.3 选择型特性效果设计规则

**规则**：选择型特性（CLASS_FEATURE_CHOICE_REGISTRY）的每个选项，`getEffects()` 只返回参与计算的真实效果。选项描述文本由特性面板 UI 展示，不在 BUFF 效果中重复。DM 可通过 BUFF 编辑器覆盖任何选项的效果。

---

## 八、分步实施计划

> **核心约束**：每 1-2 周跑一次团，每个步骤必须独立可用，不能大改到无法使用。
>
> **排序原则**：玩家体验优先 → 先做跑团中直接感受到的功能，再做基础设施。

---

### Step 1：BUFF 状态栏精简（P0，~2 天）✅ 已完成

**目标**：来源标签显示特性名（如"荒野变形"），描述精简为一行核心摘要。

**改动范围**：
- `effectMapping.js`：sourceLabel 改为 `f.name`（特性名）
- `classFeatureDefaultBuffs.js`：精简 custom_condition 描述文本
- `classFeatureChoiceRegistry.js`：选项 getEffects() 移除冗余 custom_condition

**交付物**：BUFF 状态栏显示"荒野变形"而非"德鲁伊"，有真实效果时不重复描述。

---

### Step 2：BUFF 特性筛选（P0，~2 天）✅ 已完成

**目标**：按四条件重新评估德鲁伊 14 个 BUFF 条目，移除纯描述性条目（不进 BUFF 编辑器），保留有数值收益的。

**改动范围**：
- `classFeatureDefaultBuffs.js`：移除纯 custom_condition 条目（如 archdruid、epic_boon_druid 等纯叙述性的）
- 保留有实际效果的条目（wild_shape 变身、lunar_form 骰子伤害、improved_circle_forms 豁免加值）

**不改动**：其他职业（本来就为空）、BUFF 编辑器。

**交付物**：德鲁伊 BUFF 条目精简为真正参与计算的条目。

**风险**：低，移除的条目不参与数值计算。

---

### Step 3 ✅：战斗手段卡片组件化

**目标**：将 CombatStatus 中 540 行的战斗手段渲染拆分为独立子组件。

**完成情况**：
- 新建 `src/components/combat/WeaponAttackCard.jsx`（~200 行，物理武器攻击卡片）
- 新建 `src/components/combat/SpellAttackCard.jsx`（~200 行，法术攻击卡片）
- 新建 `src/components/combat/ItemUseCard.jsx`（~400 行，道具攻击卡片，含爆炸品/卷轴/法器三个子组件）
- `CombatStatus.jsx`：战斗手段区域改为构建共享 ctx + 渲染子组件，兜底法术卡片保留内联

**实际效果**：CombatStatus 从 5059 行减至 4728 行（-331 行），渲染区域从 ~447 行内联 JSX 缩减为 ~116 行组件调用。卡片组件内部自行计算显示值（射程/命中/伤害），通过 ctx 共享回调和状态。

**不改动**：添加/编辑弹窗（Step 4 拆）、数据模型、计算引擎。

---

### Step 4：战斗手段添加弹窗拆分（~3 天）

**目标**：将 550 行的添加战斗手段弹窗拆分为分步组件。

**改动范围**：
- 新建 `src/components/combat/AddMeanTypeStep.jsx`（类型选择）
- 新建 `src/components/combat/AddWeaponStep.jsx`（武器表单）
- 新建 `src/components/combat/AddSpellStep.jsx`（法术表单）
- 新建 `src/components/combat/AddItemStep.jsx`（物品表单）
- `CombatStatus.jsx`：弹窗改为分步渲染

**不改动**：武器/法术/物品的数据结构、计算引擎。

**交付物**：弹窗代码拆分为 4 个独立文件，每个 <150 行。

**风险**：低，纯重构。

---

### Step 5：武器攻击面板重构（Phase 1A，~4 天）

**目标**：武器攻击卡片显示 `命中 +7 | 伤害 1d8+4 挥砍`，点击即投骰，显示完整攻击流程。

**改动范围**：
- `WeaponAttackCard.jsx`（Step 3 已创建）：完善攻击流程 UI
- 新建 `src/components/combat/AttackResultPanel.jsx`：攻击结果展示
- `BottomNav.jsx`：骰子结果区增加攻击/伤害标签

**不改动**：BUFF 计算、装备数据结构。

**交付物**：玩家点击武器卡片 → 投攻击骰 → 显示命中/未命中 → 投伤害骰 → 显示伤害。

**风险**：中，涉及骰子调用链路。需充分测试。

---

### Step 6：法术位消耗追踪（Phase 1A，~3 天）

**目标**：法术卡片上显示 `3环 ■■■□ 2/3`，点击消耗/恢复，短休/长休自动恢复。

**改动范围**：
- `CombatStatus.jsx`：法术位区域增加点击交互
- `CharacterSpells.jsx`：法术列表增加法术位指示器

**不改动**：法术数据结构、BUFF 系统。

**交付物**：法术位可视化消耗/恢复。

**风险**：低，现有法术位数据已有，只改 UI 交互。

---

### Step 7：主动技能注册表 + 数据层（Phase 1B，~4 天）

**目标**：创建 ACTIVE_ABILITY_REGISTRY，注册首批主动技能（荒野变形、回气、圣疗、巫师杀手）。

**改动范围**：
- 新建 `src/data/activeAbilityRegistry.js`
- 新建 `src/lib/activeAbilityEngine.js`（条件检查 + 资源扣除 + 效果执行）
- `classResourceRules.js`：确认资源键兼容

**不改动**：BUFF 系统、战斗系统、现有 UI。

**交付物**：数据层就绪，可通过 API 查询角色的主动技能列表并执行。

**风险**：低，纯新增代码。

---

### Step 8：主动技能按钮栏 UI（Phase 1B，~4 天）

**目标**：在 CombatStatus 中新增技能栏区域，显示主动技能按钮。

**改动范围**：
- 新建 `src/components/combat/ActionPanel.jsx`（技能按钮网格）
- `CombatStatus.jsx`：在战斗手段区上方插入 ActionPanel
- 按钮状态：可用=亮色、已用=灰色、条件不满足=红色

**不改动**：BUFF 系统、武器/法术逻辑。

**交付物**：玩家看到主动技能按钮 → 点击 → 执行效果 → 扣除资源。

**风险**：中，涉及资源状态更新。需测试短休/长休恢复联动。

---

### Step 9：野蛮人 + 战士 BUFF 补全（Phase 3，~3 天）

**目标**：为野蛮人和战士补全满足四条件的职业特性 BUFF。

**改动范围**：
- `classFeatureDefaultBuffs.js`：新增野蛮人（无甲防御=AC、危险感知=先攻、持久狂暴=HP、狂暴=伤害加值）和战士（回气=heal、动作涌潮=额外动作、改进暴击=暴击范围）条目

**不改动**：BUFF 编辑器、计算引擎。

**交付物**：野蛮人和战士的数值型特性自动出现在 BUFF 栏。

**风险**：低，纯数据新增。

---

### Step 10：吟游诗人 + 圣武士 + 游侠 BUFF 补全（Phase 3，~3 天）

同上模式，补全三个职业。

---

### Step 11：游荡者 + 武僧 + 魔契师 BUFF 补全（Phase 3，~3 天）

同上模式。

---

### Step 12：施法职业 BUFF 补全（Phase 3，~4 天）

术士/法师/牧师 + 自设职业（魂灵学者/狂念者/火铳手/器魂术士/圣魂之刃/岚御法师/武道家/无相影门）。

---

### Step 13：种族 BUFF 系统（Phase 3，~4 天）

**目标**：种族特性（黑暗视觉、天生武器、抗性、属性加值等）配置为真实 BUFF。

**改动范围**：
- 新建 `src/data/raceDefaultBuffs.js`
- `effectMapping.js`：新增 `getBuffsFromRace()` 函数
- 角色创建流程增加种族选择

**不改动**：BUFF 编辑器（已有足够效果类型支持）。

**交付物**：选择种族后，种族特性自动出现在 BUFF 栏并参与计算。

**风险**：中，涉及角色数据模型扩展（需增加 race 字段）。

---

### Step 14：BuffForm 编辑器 UX 优化（~3 天）

**目标**：解决 2.3 节发现的 UX 问题。

**改动范围**：
- 效果大类选择：长下拉改为分组按钮网格
- 熟练检查表：27 个 checkbox 改为搜索多选（带已选计数）
- 变身编辑器：拆分为 2 步（基础 → 高级）
- 选择型子效果：3 层弹窗改为内嵌编辑区

**不改动**：数据结构、计算引擎。

**交付物**：BUFF 编辑器更易用，弹窗深度不超过 2 层。

**风险**：中，涉及 BuffForm 大量 UI 代码调整。

---

### Step 15：战斗日志面板（Phase 1A 补完，~3 天）

**目标**：每次攻击/施法的结果记录（命中/未命中/伤害值/关键骰），可折叠。

**改动范围**：
- 新建 `src/components/combat/CombatLogPanel.jsx`
- `CombatStatus.jsx`：在战斗手段区下方增加日志区

**交付物**：战斗过程有迹可循。

---

### Step 16：背景 BUFF + 武器精通 BUFF（Phase 3 补完，~2 天）

背景熟练 + 2024 武器精通特性映射。

---

### Step 17：种族数据表 + 展示页面（P0，~4 天）

**目标**：当前完全没有种族数据表和展示页面。需从零构建种族数据体系，包含数据录入、页面展示、角色选择。

**数据模型**：
```
Race = {
  id, name,                     // 如 "人类"、"精灵"、"提夫林"
  size, speed,                  // 体型、速度
  abilityBonuses,               // 属性加值（2024：+2/+1 或固定分配）
  traits[],                     // 种族特性列表
  darkvision,                   // 黑暗视觉（如有）
  resistances,                  // 抗性（如有）
  proficiencies,                // 熟练项（武器/工具/技能）
  spells,                       // 种族法术（如有）
  subraces[],                   // 亚种（如有）
  buffs[],                      // 关联 BUFF 效果
}
```

**功能需求**：
- 新建 `/more/races` 页面：种族列表 + 详情查看
- DM 可录入/编辑种族数据（CRUD）
- 玩家可在角色卡中选择种族，种族特性自动生效
- 硬编码常见种族数据（人类/精灵/矮人/半身人/龙裔/提夫林等）
- 种族特性中有数值收益的自动配置为 BUFF

**改动范围**：
- 新建 `src/data/raceData.js`：硬编码种族数据
- 新建 `src/pages/RaceLibrary.jsx`：种族展示页面
- `App.jsx`：增加路由
- `effectMapping.js`：新增 `getBuffsFromRace()`
- `CharacterSheet.jsx`：角色卡增加种族选择区域

**不改动**：BUFF 编辑器、计算引擎（已有足够效果类型）。

**交付物**：种族数据表 + 展示页面 + 角色卡种族选择。

**风险**：中，涉及角色数据模型扩展（增加 race 字段）和 BUFF 管线新增来源。

---

### Step 18：背景数据表 + 展示页面（P0，~3 天）

**目标**：当前完全没有背景数据表和展示页面。需从零构建背景数据体系。

**2024 规则说明**：官方 2024 规则中背景与起源专长（Origin Feat）和属性加值绑定。本项目已解耦——背景仅提供技能熟练、工具熟练、语言和特性描述，起源专长和属性加值由玩家独立选择。DM 可通过房规决定是否启用官方绑定。

**数据模型**：
```
Background = {
  id, name,                     // 如 "侍僧"、"罪犯"、"贤者"
  skillProficiencies[],         // 技能熟练
  toolProficiencies[],          // 工具熟练
  languages[],                  // 语言
  equipment[],                  // 起始装备
  feature,                      // 背景特性描述
  suggestedCharacteristics,     // 建议性格特征
  originFeat,                   // 2024 关联起源专长（可选，DM 控制是否绑定）
  abilityBonuses,               // 属性加值（可选，DM 控制是否绑定）
  buffs[],                      // 关联 BUFF 效果
}
```

**功能需求**：
- 新建 `/more/backgrounds` 页面：背景列表 + 详情查看
- DM 可录入/编辑背景数据
- 玩家可在角色卡中选择背景，熟练项自动生效
- 硬编码常见背景数据（侍僧/罪犯/民间英雄/贤者/士兵/流浪儿等）
- 背景熟练自动配置为 BUFF

**改动范围**：
- 新建 `src/data/backgroundData.js`：硬编码背景数据
- 新建 `src/pages/BackgroundLibrary.jsx`：背景展示页面
- `App.jsx`：增加路由
- `effectMapping.js`：新增 `getBuffsFromBackground()`
- `CharacterSheet.jsx`：角色卡增加背景选择区域

**不改动**：BUFF 编辑器、计算引擎。

**交付物**：背景数据表 + 展示页面 + 角色卡背景选择 + 熟练项自动 BUFF。

**风险**：低，与种族系统模式一致。

---

### Step 19：属性生成系统（P0，~3 天）

**目标**：新建角色时需要生成六大属性值。提供两种规则供选择，点数池可配置。

**生成方式**：

| 方式 | 说明 | 适用场景 |
|------|------|---------|
| 购点法（Point Buy） | 默认 27 点分配（数值可配置），属性 8-15 | 平衡团、标准团 |
| 天命投掷（Dice Roll） | 投 4d6 去最低，共投 6 次 | 英雄团、高自由度团 |

**功能需求**：
- 角色创建向导中集成属性生成步骤
- 购点法：6 个属性步进器，显示剩余点数，属性范围 8-15（可配置上限）
- 天命投掷：投 4d6 去最低 × 6 次，结果可拖拽分配到六个属性
- DM 可配置购点法总点数（默认 27）
- 种族属性加值在生成后叠加显示

**改动范围**：
- 新建 `src/components/AbilityScoreGenerator.jsx`：属性生成组件
- 新建 `src/lib/pointBuy.js`：购点法计算逻辑
- 新建 `src/lib/diceRoll.js`：天命投掷逻辑
- 集成到角色创建向导（Step 20）

**不改动**：BUFF 系统、计算引擎。

**交付物**：角色创建时可选购点法或天命投掷生成属性。

**风险**：低，纯新增组件。

---

### Step 20：角色创建向导（P0，~5 天）

**目标**：新建角色时提供分步引导，参考官方玩家手册（PHB）的角色创建流程。新手不再迷茫。

**创建步骤**（参考 PHB 第 1 章）：

| 步骤 | 内容 | 数据来源 |
|------|------|---------|
| 1. 基本信息 | 姓名、性别、阵营、外观描述、性格特征、理想、牵绊、缺陷（空白填入项） | 用户输入 |
| 2. 选择种族 | 种族列表 + 特性预览 + 属性加值 | Step 17 种族数据表 |
| 3. 选择职业 | 职业列表 + 特性预览 | 现有职业数据 |
| 4. 生成属性 | 购点法 / 天命投掷 | Step 19 属性生成系统 |
| 5. 选择背景 | 背景列表 + 熟练项预览 | Step 18 背景数据表 |
| 6. 起始装备 | 职业装备包 / 逐个选购 + 法术材料/组件追踪 | 装备数据 |
| 7. 确认创建 | 总览所有选择，一键创建 | — |

**功能需求**：
- 分步向导 UI，每步可返回修改
- 每步显示当前选择的预览和数值影响
- 属性加值实时显示（种族 + 背景 + 购点/投掷）
- 最后一步总览：属性/熟练/HP/AC 等关键数值预览
- DM 可配置是否跳过某些步骤（如允许直接输入属性值）

**改动范围**：
- 新建 `src/components/CharacterCreationWizard.jsx`：向导主组件
- 新建 `src/components/creation/` 目录：每步一个子组件
- `Dashboard.jsx`：新建角色按钮改为打开向导
- `App.jsx`：增加路由

**不改动**：现有角色卡（CharacterSheet）、BUFF 系统。

**交付物**：新手可从零开始一步步创建完整角色。

**风险**：中，涉及多个数据源整合和步骤间状态管理。依赖 Step 17-19。

---

### Step 21：专注管理 + 灵感追踪（~3 天）

**目标**：实现专注状态追踪和 DM 灵感系统。施法者每场战斗的核心机制。

**专注管理**：
- 角色卡追踪"正在专注"状态（当前专注的法术名称）
- 受伤时自动提示进行体质豁免检定（DC = 10 或伤害值的一半，取高）
- 专注断裂时关联法术自动失效（移除法术效果）
- 角色卡上明显显示当前专注状态
- 强化专注的 BUFF 增益（如专注豁免加值，已有 `concentration_save_enhance` 效果类型）

**灵感追踪**：
- 角色卡上增加灵感开关（有/无灵感）
- DM 可授予/撤销灵感
- 玩家消耗灵感时投 d20 优势骰
- 吟游诗人激励资源与 DM 灵感联动（诗人激励骰 + DM 灵感骰分别追踪）
- 针对诗人增益：部分诗人特性与灵感投相关，需要统一追踪

**改动范围**：
- 新建 `src/components/ConcentrationTracker.jsx`：专注状态组件
- 新建 `src/components/InspirationTracker.jsx`：灵感开关组件
- `CharacterSheet.jsx`：集成专注和灵感追踪
- `CombatStatus.jsx`：受伤时触发专注豁免提示
- 角色数据模型增加 `concentration` 和 `hasInspiration` 字段

**不改动**：BUFF 计算引擎、战斗系统核心逻辑。

**交付物**：施法者可追踪专注状态，受伤自动提示豁免；灵感可被授予/消耗。

**风险**：中，涉及法术效果与专注状态联动。

---

### Step 22：视觉系统（~3 天）

**目标**：追踪角色的各种特殊视觉能力，影响探索交互和战斗。

**视觉类型**：

| 类型 | 来源 | 说明 |
|------|------|------|
| 黑暗视觉（Darkvision） | 种族基础值、部分职业/法术 | 在黑暗中视为微光照明 |
| 真实视觉（Truesight） | 稀有，通常法术效果 | 看穿变形、看穿幻象、看到灵体 |
| 盲视（Blindsight） | 稀有，特殊生物特性 | 无需视觉感知周围 |
| 魔鬼视觉（Devil's Sight） | 仅魔契师（Warlock） | 在魔法黑暗中正常视物 |

**功能需求**：
- 角色卡显示所有视觉能力及其范围（尺）
- 种族提供基础黑暗视觉（如精灵 60 尺），职业/法术可叠加或覆盖
- 视觉能力配置为 BUFF 效果（新增 `vision` 效果类型到 buffTypes）
- BUFF 管线中计算最终视觉能力（取最大值）
- DM 可在编辑模式下修改角色视觉能力

**改动范围**：
- `buffTypes.js`：新增 `vision` 效果类型（visionType + range）
- `buffEffectRegistry.js`：注册视觉效果的计算规则
- `useBuffCalculator.js`：合并多个来源的视觉能力
- `CharacterSheet.jsx`：角色卡显示视觉能力区域
- `raceData.js`（Step 17）：种族数据中包含视觉能力
- `classFeatureDefaultBuffs.js`：魔契师魔鬼视觉等职业视觉 BUFF

**不改动**：BUFF 编辑器核心、战斗系统。

**交付物**：角色卡显示黑暗视觉/真实视觉/盲视/魔鬼视觉及范围，多来源自动取最大值。

**风险**：低，新增 BUFF 效果类型 + UI 展示。

---

### Step 23：战斗特殊动作 + 双武器战斗（~4 天）

**目标**：补全 PHB 核心战斗动作——擒抱/推撞，以及双武器战斗规则。

**擒抱（Grapple）**：
- 特殊近战攻击：运动（Athletics）检定 vs DC（DM 告知结果）
- 成功后自身获得"正在擒抱"状态标记
- 被擒抱目标可每回合再次尝试挣脱（DM 告知结果后手动更新状态）

**推撞（Shove）**：
- 特殊近战攻击：运动检定 vs DC（DM 告知结果）
- 成功可推倒目标（倒地 prone）或推开 5 尺

**双武器战斗（Two-Weapon Fighting）**：
- 副手攻击使用附赠动作
- 副手伤害不加属性修正（除非有双武器战斗风格）
- 需要两把轻武器（light property）
- 副手攻击卡片独立显示，正确计算伤害

**增益支持**：
- 增强擒抱/推撞的 BUFF（如运动检定加值）
- 双武器战斗风格正确应用

**改动范围**：
- 新建 `src/components/combat/GrappleShovePanel.jsx`：擒抱/推撞动作面板
- `CombatStatus.jsx`：集成特殊动作按钮
- `buffTypes.js`：新增 `grappled` 状态追踪
- 武器攻击计算：增加双武器副手攻击逻辑
- `fightingStyles.js`：确认双武器战斗风格效果正确

**不改动**：BUFF 计算引擎核心、武器数据结构。

**交付物**：战斗中可使用擒抱/推撞/双武器攻击，状态正确追踪。

**风险**：中，涉及战斗状态管理和多种动作交互。

---

### Step 24：法术材料管理（~2 天）

**目标**：追踪施法材料组件的消耗，特别是昂贵材料自动扣除。

**功能需求**：
- 角色数据增加材料 pouch（施法材料袋）和组件追踪
- 施法焦点（Focus）追踪：法杖/水晶/圣徽等
- 施法时自动检测法术是否有材料组件需求
- 有金币成本的昂贵材料自动消耗（转换为金币扣除）
- 材料 pouch 视为无限普通材料（无需追踪蝙蝠粪便等普通材料）
- 昂贵材料单独追踪数量/金币价值

**改动范围**：
- 角色数据模型增加 `spellComponents` 字段
- `spellData.js`：法术数据中标记昂贵材料成本
- `CombatStatus.jsx` / 法术施展流程：施法时检查并消耗材料
- 仓库/背包系统：昂贵材料与金币联动

**不改动**：法术数据结构核心、BUFF 系统。

**交付物**：施法时昂贵材料自动消耗，施法焦点正确追踪。

**风险**：低，纯资源追踪。

---

### Step 25：短休系统改进 + 生命骰（~3 天）

**目标**：短休时支持花生命骰回血，并让长/短休按钮在 UI 中更明显。

**生命骰系统**：
- 显示角色当前剩余生命骰数量（如 5/8 d8）
- 短休时可选择花费生命骰，逐个投骰回血
- 每次花费显示投骰结果 + CON 修正 + 回复 HP
- 长休自动恢复一半已消耗的生命骰
- 多职业角色正确追踪不同职业的生命骰类型

**UI 改进**：
- 长休/短休按钮在角色卡中更加醒目（目前不够明显）
- 短休面板整合：生命骰花费 + 职业资源恢复
- 长休面板整合：HP 回满 + 资源恢复 + 生命骰恢复 + 死亡豁免重置

**改动范围**：
- 新建 `src/components/RestPanel.jsx`：统一的休息面板
- `CombatStatus.jsx`：短休/长休按钮移至更显眼位置
- `CharacterSheet.jsx`：休息入口整合
- 角色数据模型增加 `hitDiceUsed` 追踪

**不改动**：BUFF 系统、职业数据结构（hitDice 已有）。

**交付物**：短休可花生命骰回血，长/短休按钮醒目易用。

**风险**：低，现有短休系统基础上扩展。

---

### Step 26：升级引导系统（Phase 4，~5 天）

**目标**：升级时自动提示：属性提升/新专长/新法术/新职业特性/新主动技能。

**改动范围**：
- 新建 `src/components/LevelUpGuide.jsx`
- `CharacterSheet.jsx`：等级变化时触发引导面板

**交付物**：升级不再遗漏选择。

---

### Step 27：日志系统基础（Phase 5，~5 天）

**目标**：日志数据模型 + 存储 + 基础页面 UI。

**改动范围**：
- 新建 `src/data/journalTypes.js`
- 新建 `src/lib/journalStore.js`
- 新建 `src/pages/Journal.jsx`
- `App.jsx`：增加路由
- `BottomNav.jsx`：可选增加日志 Tab 或放入"更多"

**交付物**：可创建/查看五种类型的日志条目。

---

### Step 28：DM 编辑模式基础框架（Phase 6，~5 天）

**目标**：实现统一编辑模式开关 + 编辑态 UI 基础设施。

**改动范围**：
- 新建 `src/components/DMEditToggle.jsx`（编辑模式开关）
- 新建 `src/contexts/DMEditContext.jsx`（编辑状态上下文）
- `Layout.jsx`：注入 DM 编辑上下文
- 各页面：条件渲染编辑图标

**交付物**：DM 可开启编辑模式，看到铅笔/齿轮图标。

---

### Step 29：DM 编辑 — 专长/职业特性管理（Phase 6，~5 天）

在编辑模式下，DM 可新增/修改专长和职业特性的名称、描述、BUFF 效果。

---

### Step 30：DM 编辑 — 武器/法术/物品管理（Phase 6，~4 天）

在编辑模式下，DM 可新增/修改武器、法术、魔法物品数据。

---

### Step 31：DM 编辑 — 种族/背景/怪物管理（Phase 6，~4 天）

在编辑模式下，DM 可新增/修改种族、背景、怪物数据。

---

### Step 32：房规可视化开关（Phase 6，~3 天）

可视化启用/禁用特定规则条目。

---

### Step 33：商店系统（Phase 4，~5 天）

DM 设定商店库存，玩家浏览/购买/出售物品。

---

### Step 34：数据持久化加固（Phase 7，~5 天）

自动备份策略 + 数据导出/导入。

---

### Step 35：版本历史 + 离线模式（Phase 7，~7 天）

角色数据变更历史 + Service Worker 缓存。

---

### Step 36：野兽/生物库用户录入（Phase 4，~4 天）

**目标**：现有生物库（CreatureLibraryManager）仅 DM 可管理。扩展为玩家可录入/浏览野兽数据，支持荒野变形等特性直接引用。

**功能需求**：
- DM 录入完整生物数据（AC/HP/速度/攻击/特性/CR）
- 玩家可查看已录入的生物列表（DM 可控制可见范围）
- 荒野变形时直接从生物库选择形态，自动填充变身数据
- 生物数据支持 DM 覆盖（优先级：DM 配置 > 硬编码默认）

**改动范围**：
- `CreatureLibraryManager.jsx`：扩展权限模型，增加玩家浏览入口
- 新建 `src/data/creatureLibrary.js`：硬编码常见野兽数据（棕熊/狼/蜘蛛等）
- `effectMapping.js`：荒野变形引用生物库数据
- BUFF 变身编辑器：关联生物库下拉选择

**不改动**：BUFF 计算引擎、战斗系统。

**交付物**：DM 可录入野兽，玩家可在荒野变形时选择生物库中的形态。

**风险**：中，涉及变身系统与生物库数据联动。

---

### Step 37：召唤系统（Phase 4，~5 天）

**目标**：实现召唤物管理——召唤物是独立实体，拥有自己的数值（AC/HP/攻击/动作），区别于荒野变形的"自身变身"。

**召唤类型**：

| 类型 | 示例 | 消耗 |
|------|------|------|
| 生命消耗型 | 星界分身（消耗一半 HP） | 当前 HP ÷ 2 |
| 资源置换型 | 召唤魔宠（消耗法术位/资源） | 法术位 + 材料 |
| 次数限制型 | 召唤野兽同伴 | 长休恢复次数 |
| 法术召唤型 | 召唤元素生物 | 对应环阶法术位 |

**数据模型**：
```
SummonedCreature = {
  id, name, source,            // 来源特性
  ac, hp, maxHp,               // 独立数值
  speed, abilities,            // 属性
  attacks[],                   // 攻击动作
  actions[],                   // 特殊动作
  duration,                    // 持续时间
  ownerId,                     // 召唤者角色 ID
  conditions[],                // 状态条件
}
```

**功能需求**：
- 召唤面板：显示可用召唤技能 + 消耗 + 当前活跃召唤物
- 召唤物卡片：独立显示 AC/HP/攻击，可投骰
- 消耗自动扣除（HP/资源/次数）
- 持续时间追踪（回合/分钟/直到长休）
- DM 可自定义召唤物数据（通过 BUFF 编辑器或 DM 编辑模式）

**改动范围**：
- 新建 `src/data/summonRegistry.js`：注册召唤技能（消耗/持续时间/默认数据）
- 新建 `src/components/combat/SummonPanel.jsx`：召唤管理面板
- `CombatStatus.jsx`：集成召唤面板
- 主动技能系统（Step 7-8）：召唤作为主动技能触发

**不改动**：BUFF 计算引擎、变身系统、武器/法术逻辑。

**交付物**：玩家可召唤生物，召唤物有独立数值和动作，消耗正确扣除。

**风险**：高，涉及多个系统联动（资源/战斗/主动技能）。需分阶段实现。

---

### Step 38：3D 骰子动画优化（~3 天）

**目标**：优化 `ThreeDiceOverlay.jsx`（1046 行，vanilla Three.js + 自写物理引擎）的动画流畅度，解决最后几帧卡顿问题。

**问题分析**：
- 骰子物理引擎自写（重力/弹跳/角速度/空气阻尼/碰撞检测），非专业物理引擎
- `showFinal` 阶段骰子强制旋转朝向结果面时，与物理模拟 settle 过程冲突导致卡顿
- Canvas 纹理渲染 + 多同时模拟可能造成 GPU 压力
- 动画结束阶段帧率下降明显

**约束**：结果面必须准确转向镜头（与数值结果一致），此行为不可移除。

**优化方向**：
- 物理模拟阶段与 showFinal 旋转阶段严格分离：物理完全 settle 后再切换到纯动画旋转，两个阶段互不干扰
- 使用 `requestAnimationFrame` + delta time 确保帧率无关的物理模拟（当前可能是固定步长导致低速时卡顿）
- 优化碰撞检测：多骰时 2D circle-circle 碰撞改为空间分区或提前退出
- 骰子几何体复用：多骰投掷时共享 BufferGeometry 实例，减少 GPU 内存
- 结果面旋转使用平滑缓动（easeOutCubic），从物理结束到最终朝向的过渡自然流畅
- 可选：考虑迁移到 React Three Fiber（`@react-three/fiber`）+ 专业物理引擎（`cannon-es`），但工作量大，作为备选方案

**改动范围**：
- `src/components/ThreeDiceOverlay.jsx`：物理引擎 + 动画阶段重构
- 可选新建 `src/lib/dicePhysics.js`：物理模拟逻辑抽离
- 可选新建 `src/lib/diceGeometry.js`：骰子几何体工厂（复用实例）

**不改动**：`DiceRoller.jsx`（UI 面板）、`quickRollAnimation.js`（动画规格）、`PhysicsDiceOverlay.jsx`（2D 备选方案）。

**交付物**：3D 骰子动画全程流畅，无末尾卡顿感。

**风险**：中，物理引擎重构需充分测试各种骰子类型（d4-d100）的表现。

---

## 九、实施路线图总览

| 步骤 | 内容 | 预估工期 | 依赖 | 风险 |
|------|------|---------|------|------|
| 1 | BUFF 状态栏精简 ✅ | 2 天 | 无 | 极低 |
| 2 | BUFF 特性筛选 ✅ | 2 天 | Step 1 | 低 |
| 3 | 战斗手段卡片组件化 | 3 天 | 无 | 低 |
| 4 | 战斗手段添加弹窗拆分 | 3 天 | Step 3 | 低 |
| 5 | 武器攻击面板重构 | 4 天 | Step 3 | 中 |
| 6 | 法术位消耗追踪 | 3 天 | 无 | 低 |
| 7 | 主动技能注册表 + 数据层 | 4 天 | 无 | 低 |
| 8 | 主动技能按钮栏 UI | 4 天 | Step 7 | 中 |
| 9 | 野蛮人 + 战士 BUFF | 3 天 | Step 2 | 低 |
| 10 | 吟游诗人 + 圣武士 + 游侠 BUFF | 3 天 | Step 9 | 低 |
| 11 | 游荡者 + 武僧 + 魔契师 BUFF | 3 天 | Step 10 | 低 |
| 12 | 施法职业 BUFF | 4 天 | 无 | 低 |
| 13 | 种族 BUFF 系统 | 4 天 | Step 17, 12 | 中 |
| 14 | BuffForm UX 优化 | 3 天 | 无 | 中 |
| 15 | 战斗日志面板 | 3 天 | Step 9 | 低 |
| 16 | 背景 + 武器精通 BUFF | 2 天 | Step 18, 13 | 低 |
| 17 | 种族数据表 + 展示页面 | 4 天 | 无 | 中 |
| 18 | 背景数据表 + 展示页面 | 3 天 | 无 | 低 |
| 19 | 属性生成系统（购点/投掷） | 3 天 | 无 | 低 |
| 20 | 角色创建向导（含性格特征） | 5 天 | Step 17-19 | 中 |
| 21 | 专注管理 + 灵感追踪 | 3 天 | 无 | 中 |
| 22 | 视觉系统（黑暗视觉/真实视觉/盲视/魔鬼视觉） | 3 天 | Step 17 | 低 |
| 23 | 战斗特殊动作 + 双武器战斗 | 4 天 | Step 9 | 中 |
| 24 | 法术材料管理 | 2 天 | 无 | 低 |
| 25 | 短休系统改进 + 生命骰 | 3 天 | 无 | 低 |
| 26 | 升级引导系统 | 5 天 | Step 12 | 中 |
| 27 | 日志系统基础 | 5 天 | 无 | 低 |
| 28 | DM 编辑模式框架 | 5 天 | 无 | 中 |
| 29 | DM 编辑 — 专长/职业 | 5 天 | Step 28 | 中 |
| 30 | DM 编辑 — 武器/法术 | 4 天 | Step 29 | 低 |
| 31 | DM 编辑 — 种族/背景/怪物 | 4 天 | Step 30 | 低 |
| 32 | 房规可视化开关 | 3 天 | Step 28 | 低 |
| 33 | 商店系统 | 5 天 | Step 28 | 中 |
| 34 | 数据持久化加固 | 5 天 | 无 | 中 |
| 35 | 版本历史 + 离线模式 | 7 天 | Step 34 | 高 |
| 36 | 野兽/生物库用户录入 | 4 天 | Step 2 | 中 |
| 37 | 召唤系统 | 5 天 | Step 7-8 | 高 |
| 38 | 3D 骰子动画优化 | 3 天 | 无 | 中 |

**推荐执行顺序**（按跑团周期排列）：
- **第 1-2 周**：Step 1-2（BUFF 精简 + 筛选）→ 立刻改善现有体验 ✅ 已完成
- **第 3-4 周**：Step 3-5（战斗卡片重构 + 武器攻击）→ 战斗中可直接用
- **第 5-6 周**：Step 6-8（法术位 + 主动技能）→ 跑团体验飞跃
- **第 7-12 周**：Step 9-16（BUFF 补全 + UX 优化）→ 基础设施完善
- **第 13-14 周**：Step 17-20（种族表 + 背景表 + 属性生成 + 创建向导）→ 新手可建角色
- **第 15-16 周**：Step 21-25（专注/灵感/视觉/战斗动作/短休）→ 战斗规则补全
- **第 17-22 周**：Step 26-33（养成/日志/DM 编辑/商店）→ 功能全面
- **第 23-26 周**：Step 34-35（持久化加固）→ 长期保障
- **可并行**：Step 36（生物库录入）→ 配合荒野变形使用
- **可并行**：Step 37（召唤系统）→ 需 Step 7-8 完成
- **可并行**：Step 38（3D 骰子动画优化）→ 随时可做，改善投骰体验

每个 Step 独立可部署，不影响现有功能。
