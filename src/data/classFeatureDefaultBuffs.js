/**
 * 职业特性默认 BUFF 效果（硬编码回退）
 *
 * 已清空：所有职业特性不再硬编码 BUFF 条目。
 * 特性是否出现在 BUFF 栏，取决于 DM 是否在职业卡/专长卡等卡槽中手动添加功能卡。
 * 有功能卡 → 出现在 BUFF 栏；无功能卡 → 不出现。
 *
 * Key 格式：`${职业}|${子职|''}|${featureId}`
 */

/* ════════════════════════════════════════════════════════════════════ */
export const HARDCODED_CLASS_FEATURE_BUFFS = {}
